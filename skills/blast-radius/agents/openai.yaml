interface:
  display_name: "Blast Radius"
  short_description: "Map dependencies and change impact before shipping"
  default_prompt: "Use $blast-radius to map the impact and dependencies of this change."
